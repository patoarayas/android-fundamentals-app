<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.ucn.disc.dsm.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">

        <activity
            android:name=".words.NewWordActivity"
            android:label="New words"
            android:parentActivityName=".words.WordActivity" />

        <activity
            android:name=".words.WordActivity"
            android:label="WordsActivity"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".task.TaskActivity"
            android:label="Task Activity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".counter.CounterActivity"
            android:label="Counter Activity"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>